[project]
name = "strive-ai"
version = "0.0.0"
description = "AI service Strive Academy — ekstraksi, LLM, skor ATS deterministik, render."
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py312"
