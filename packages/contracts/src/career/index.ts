// Skema zod domain `career` — docs/PRD.md §10.3 "Karir & AI" (bagian CV +
// Prompt Lab) + §7 E8/E13. `GET /ai/jobs/:id` TIDAK dicakup di sini — di
// luar daftar endpoint yang diminta cakupan kerja F-08 (lihat laporan).
export * from './cv';
export * from './prompt-lab';
