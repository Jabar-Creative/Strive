import { PageStub } from '@/components/page-stub';

export default function Page() {
  return (
    <PageStub
      title="Konsol superadmin"
      item="SA-04"
      dev="B"
      prd="PRD §7 E9 (Retool, bukan UI React sendiri)"
    />
  );
}
